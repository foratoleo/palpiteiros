/**
 * Transition Components Export
 *
 * Central export point for all transition components.
 *
 * @example
 * ```tsx
 * import { PageTransition, StaggerChildren, SlideIn } from '@/components/transitions'
 * ```
 */

export {
  PageTransition,
  StaggerChildren,
  StaggerItem,
  SlideIn,
  FadeIn,
  ScaleIn,
  pageVariants,
  staggerContainer,
  staggerItem,
  presets,
} from "./page-transition";
